const fs = require('fs');
const Sistema = require('./js/sistema.js'); // Assuming I can run this in node, or I'll use a different way

// Actually, I can't easily run Sistema.js in node without mocking.
// I'll just use run_command with a clever echo.
